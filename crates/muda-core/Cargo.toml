[package]
name = "muda-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Lossless JPEG/PNG/WebP container rewrite that strips identity metadata"
rust-version = "1.85"

[dependencies]
img-parts = "0.4"
kamadak-exif = "0.6"

[dev-dependencies]
image = { version = "0.25", default-features = false, features = ["jpeg", "png"] }
kamadak-exif = "0.6"
